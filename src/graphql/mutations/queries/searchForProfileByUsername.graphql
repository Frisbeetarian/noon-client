query SearchForProfileByUsername($username: String!) {
    searchForProfileByUsername(username: $username){
        uuid
        username
        name
        userId
        updatedAt
        createdAt
    }
}
