mutation UploadImage(
  $profileUuid: String!
  $conversationUuid: String!
  $file: Upload!
) {
  uploadImage(
    profileUuid: $profileUuid
    conversationUuid: $conversationUuid
    file: $file
  ) {
    uuid
  }
}
