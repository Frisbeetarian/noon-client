mutation SaveGroupMessage(
  $message: String!
  $conversationUuid: String!
  $type: String!
  $src: String!
) {
  saveGroupMessage(
    message: $message
    conversationUuid: $conversationUuid
    type: $type
    src: $src
  ) {
    uuid
  }
}
