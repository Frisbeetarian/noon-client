mutation SaveMessage(
  $message: String!
  $conversationUuid: String!
  $to: String!
  $type: String!
  $src: String!
) {
  saveMessage(
    message: $message
    conversationUuid: $conversationUuid
    to: $to
    type: $type
    src: $src
  ) {
    uuid
  }
}
